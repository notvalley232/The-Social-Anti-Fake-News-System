{
  "name": "anti-fakenews-api-data",
  "version": "1.0.0",
  "description": "JSON Server API for Anti-Fake News System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3001 --host 0.0.0.0",
    "build": "node scripts/build-db.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["json-server", "api", "mock-data", "anti-fakenews"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
